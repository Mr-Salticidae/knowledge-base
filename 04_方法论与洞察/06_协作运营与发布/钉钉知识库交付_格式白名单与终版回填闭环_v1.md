---
tags: [类型/协作工具链, 主题/教程制作]
---
# 钉钉知识库交付：格式白名单 + 终版回填闭环

> 入档：2026-07-17
> 来源：《实战6-自动化口播剪辑》附件版并入钉钉知识库母文档「自动化口播剪辑」节的交付全程（教程本体见 [[完结项目反推学员教程_大纲先行两级审核_v1]]）
> 验证状态：⚠️ 首次走通（一轮上级审核过审、终版已回填 git）；格式白名单已被作者确认为**后续钉钉交付的固定规则**

---

## 事实记录（不可修改区）

- 目标：教程正文定稿后并入公司钉钉知识库母文档，上级在钉钉上做终审
- API 路线搁置：钉钉推送脚本骨架已就绪（`ai-editing-course/scripts/dingtalk_push.py`，凭证走 `.dingtalk.env` 不入库），卡在企业应用权限审批，本次改走手动导入
- 手动导入前做了 4 个变体试探导入通道（单日 4 连发提交）：
  1. 附件版（小标题并入正文为纯一级文本）
  2. 公网图链版（14 处配图改 GitHub raw 直链，赌钉钉导入自动抓图）
  3. 纯文字版（去除全部 Markdown 符号，图/视频位置 🔴 文字占位，供直接粘贴）
  4. 占位标注版（保留 Markdown，仅图/视频换 🔴 占位，供知识库导入后中转）
- 母文档在钉钉上人工修正后过审。终审版 vs 仓库占位标注版的实际 diff：
  - **删除结尾「准备清单」表格**（信息由图14卡片承担，正文不再重复）
  - **Step 1 / Step 5 的示例话术从引用块改为正文段落**
  - 开场白重写为口播式完整段落（原「先看成片。」三字开头）；成片视频改为钉钉附件链接
  - 新增加粗三处：「AI 乐队」「帮我安装这几个 skill」「你说一段话，Codex 给一个结果」
  - 残留一处引用块未删（Step 4 反馈公式 `> 截图 + 圈出来 + 说想要什么`）——回填时按「不擅改过审稿」标待办上报，作者确认为漏网，当天修正线上版并同步本地，白名单零例外成立
- 审核后定下的格式规则（作者原话）：**修饰文本只能使用加粗和代码块**；引用块和表格全部去掉
- 终版 md 从钉钉下载后 verbatim 回填 `ai-editing-course/docs/实战6_自动化口播剪辑_附件版_钉钉终版.md`

## 一、方法论沉淀

### [富文本平台交付：格式白名单先行，不等审核删改]

**核心**：往钉钉这类富文本知识库交付 Markdown，语法要按平台白名单**主动收敛**，而不是全量语法交上去等审核人手动删。本次白名单：修饰只用**加粗 + 代码块**；结构层面正文段落和有序列表可用；禁引用块、禁表格、禁多级小标题（并入正文加粗）。表格的信息改走图卡片或正文列点。

**来源**：终审 diff 的两处关键删改（表格删除、引用块转正文）都是格式层修改，与内容无关——这些返工本可以在交付前收敛掉。

**操作规则**：
1. 首次给一个新平台交付，交付后拿审核版 diff 一遍，把格式层删改提炼成该平台白名单；
2. 白名单一旦确立，后续交付按白名单写母版，变体试探（直链版/纯文字版/占位版）收敛为单一母版；
3. 白名单存进 skill / 规范文档，不靠记忆。

### [终版回填闭环：平台审核版是唯一真身]

**核心**：文档在平台上被人工修改并过审后，**平台版成为 source of truth**，必须立即下载回填 git 仓库（verbatim，不做任何「顺手优化」）；否则仓库版从此漂移，下次迭代会基于过时底稿返工。

**来源**：本次终审在钉钉编辑器里完成（开场白重写、加粗调整均发生在平台侧），仓库里 4 个变体没有一个等于过审版；不回填则真身只存在于钉钉。

**操作规则**：
1. 平台过审 → 当天下载 md → 与仓库最新变体 diff → verbatim 回填为「钉钉终版」文件；
2. diff 出的格式类修改沉淀进白名单，内容类修改沉淀进复盘；
3. 回填时发现审核版自身违反已定规则（如本次残留引用块），**不擅自改过审稿**，标记待办向审核人确认。

### [导入通道不明时用变体分治，通道定了就收敛]

**核心**：不确定平台导入行为（会不会自动抓图、认不认 Markdown）时，一文多变体各赌一条通道是合理试探；但通道和白名单确认后，变体只保留历史价值，母版收敛为一个，避免多副本漂移。

**来源**：单日 4 个变体提交是试探期产物；终版落地后它们全部被「钉钉终版」取代。

## 二、一句话结论

> 富文本平台交付 = 格式白名单先行 + 终版回填闭环：交付前按白名单收敛格式，过审后平台版是唯一真身，当天下载回填，仓库才不漂移。

## 如何使用

- 下次往钉钉母文档交付：直接按 skill `07_skill存档/dingtalk-doc-delivery` 的白名单写母版，跳过变体试探；
- 遇到新的富文本平台（语雀/Notion/飞书知识库等）：套用「交付→diff 审核版→提炼白名单→回填」同一套闭环。

## 关联文档

- [[完结项目反推学员教程_大纲先行两级审核_v1]]（同项目上游：大纲与正文的两级审核；本档是其下游交付段）
- [[2026-07-16_Codex保姆级教学_代码化口播剪辑完整复盘]]（教程素材母体）
- [[复盘事实先行原则]]（回填闭环同根：先冻结事实，仓库版=事实）
- 配套 skill：[[dingtalk-doc-delivery/SKILL.md]]
- 飞书侧同类链路：[[飞书应用文档授用户编辑权_唯open_id可靠_v1]] · skill `07_skill存档/feishu-doc-publish`
- [[04_方法论与洞察索引]]
